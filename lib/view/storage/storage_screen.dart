import 'package:flutter/material.dart';

class StoragerScreen extends StatelessWidget {
  const StoragerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("this is storage Screen"),
    );
  }
}
